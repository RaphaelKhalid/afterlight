{
  "name": "afterlight-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test:integration": "vitest run tests/handler.integration.test.ts",
    "deploy:dry": "wrangler deploy --dry-run",
    "import:public": "node scripts/import-public-data.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.131.1"
  }
}
