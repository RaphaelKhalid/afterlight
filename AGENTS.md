# Implementation ownership and shared contracts

Product: Afterlight. Read ../AFTERLIGHT_EXECUTION_HANDOFF.md fully. Deadline 2026-09-13 15:00 America/Los_Angeles. No em dashes in any product/docs copy. No fabricated data. No secrets in git or browser. Public visitors cannot spend owner credits.

Architecture: React + TypeScript + Vite, Motion for React. Cloudflare Worker API + one Workflow coordinator + D1. Frontend at repository root. Backend backend/. Research scripts research/. Shared contract types shared/types.ts, coordinated by lead. Root agent owns deployment, shared contracts, integration, docs. Frontend agent owns src/, index.html and frontend config/package. Backend agent owns backend/. Experiment agent owns research/ and artifacts/experiment/. Research data will be public/data/corpus.json and public/data/demo-run.json. All code agents Luna High.

API contract (JSON): GET /api/health; GET /api/questions -> {questions,papers,edges,coverage}; GET /api/questions/:id -> question; GET /api/contracts -> {contracts}; GET /api/runs -> {runs}; GET /api/runs/:id -> run; GET /api/runs/:id/trials -> {trials}; POST /api/runs owner bearer required, body {contractId,contractHash,capUsd}; POST /api/runs/:id/control owner bearer required body {action:"pause"|"resume"|"stop"}; GET /api/attempts?questionId -> {attempts}; POST /api/telegram/invite owner only; POST /api/telegram/webhook secret header. GET read-only routes public. Frontend API base /api via Vercel rewrite. Public static data is an honest offline/read-only fallback with explicit label.

Question fields: id,title,area,status,origin,summary,whyItMatters,source{paperId,url,version,section,passage},closestWork[{paperId,finding,mismatch}],uncertainty,search{date,queries,coverage,inaccessible},executable,estimatedCostUsd,estimatedMinutes,access,position{x,y},evidenceIds. Paper: id,title,authors[],published,updated?,url,version,sourceType,abstract,topics[],verificationStatus,featured. Edge: id,source,target,type,explanation,evidenceUrl.

Run fields: id,questionId,contractId,contractHash,status,createdAt,startedAt,completedAt,capUsd,spentUsd,costStatus,completedTrials,totalTrials,failedTrials,conditions[],summary?,artifactUrl?,events[]. Trial: id,runId,caseId,condition,status,input,output,answer?,expectedAnswer?,score?,monitorVerdict?,usage?,costUsd?,createdAt,completedAt,provider,model,metadata. Include raw subject and monitor response provenance in actual artifact files. Cost status explicitly provider-returned or estimated.

Contract fields to be finalized immediately after calibration. Store actual frozen contract JSON in artifacts/experiment/contract.json; backend executes only reviewed named template. No fake completed placeholder. UI may show pending data until root imports real result. Use hash routes #/atlas, #/questions/:id, #/designer/:id, #/lab/:runId, #/results/:runId so static fallback routing is simple.

Safety caps across development/evaluation: Exa $10, OpenAI $25, OpenRouter $6. Experiment calibration max $1 initially. Track calls/returned usage, reserve upper bounds and no automatic retries after ambiguous outcomes. Original source photograph preserved in public/assets/afterlight-fireworks.png. Model/endpoint changes are explicit conditions/version changes.
